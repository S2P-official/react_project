import type { NextConfig } from "next";

const nextConfig: NextConfig = {

images: {
    domains: ['fictilecore.com'], // ✅ Allow external image URLs
  },

  // devIndicators: false
  /* config options here */
};

export default nextConfig;
